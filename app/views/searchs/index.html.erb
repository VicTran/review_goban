<% provide :title, t("product.all") %>

<div class="row bg-white">
  <div class="col-md-offset-1 col-md-10">
    <%= render "search_form" %>
    <table class="table table-bordered">
      <thead>

      </thead>
      <tbody>
        <%= render @products %>
      </tbody>
    </table>
    <div class="text-center">
      <%= paginate @products %>
    </div>
  </div>
</div>
